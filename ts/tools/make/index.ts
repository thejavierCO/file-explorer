// import fileBase from "./file"
// import dirBase from "./dir"
// import is from "../is"

class make{
    // constructor(type:string,name?:string,root?:string){
    //     switch(type){
    //         case "file":
    //             root = root+"/"+name;
    //             return new fileBase(root);
    //         case "dir":
    //             root = root+"/"+name;
    //             return new dirBase(root);
    //         default:
    //             if(is(type)==="file"){
    //                 return new make("file",type,name);
    //             }else if(is(type)==="dir"){
    //                 return new make("dir",type,name);
    //             }
    //     }
    // }
}

export default make;