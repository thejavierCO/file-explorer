import {existRoot,getRoot,getTypeElement,lastItem} from "../tools"
import {fileExplorer} from "../index";
import {explorer} from "../explorer";

export interface Icreate{}

export class create extends explorer implements Icreate {}